# ChatLog

## 功能介绍

这是一个Chrome浏览器插件（Manifest V3），用于在AI聊天网页中提升长对话的浏览效率。主要功能包括：

1. **提取用户历史对话**：只提取用户发送的历史对话内容
2. **右侧列表展示**：在页面右侧以列表形式展示历史对话，每条只显示一行摘要
3. **关键词搜索**：支持通过关键词搜索历史输入
4. **快速跳转**：点击某一条记录后快速跳转到页面中的对应位置
5. **高亮效果**：跳转后对目标内容进行短暂高亮
6. **点击跳转提示**：在列表顶部显示明确的操作提示
7. **文件消息处理**：优化处理带有文件附件的消息，显示[文件]标记
8. **可拖动调整大小**：支持左右拖动调整历史对话列表的宽度
9. **跨平台支持**：同时支持ChatGPT、豆包和Kimi平台

## 技术实现

### 核心架构

- **适配器模式**：使用PlatformAdapter接口定义平台适配方法，当前实现ChatGPTAdapter和DoubaoAdapter
- **实时监听**：使用MutationObserver监听页面变化，实时更新消息列表
- **防抖处理**：避免频繁更新导致性能问题
- **响应式设计**：适配不同屏幕尺寸

### 关键特性

1. **DOM结构稳定性**：使用data属性（如data-message-author-role="user"）识别用户消息，不依赖className
2. **可靠的跳转功能**：基于消息ID进行定位，支持多种查找方式
3. **扩展能力**：通过适配器层支持其他平台
4. **性能优化**：防抖处理、高效DOM操作
5. **文件消息处理**：智能识别文件附件，显示[文件]标记和文件名（如果可提取）
6. **可拖动调整大小**：支持通过拖动分隔条调整历史对话列表宽度
7. **响应式设计**：消息内容自动适应列表宽度，保持单行显示
8. **搜索状态保持**：页面变化时保持搜索过滤状态，不会自动跳回到显示所有消息

### 数据结构

每条消息的数据结构包含：
- id：消息唯一标识符
- content：完整内容（包含[文件]标记，如果有文件附件）
- preview：预览文本（最多100字符）
- index：消息索引
- element：消息DOM元素

## 安装说明

1. 克隆或下载本项目到本地
2. 打开Chrome浏览器，访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择本项目目录
6. 插件将自动安装并在ChatGPT页面生效

## 后续扩展

1. **支持其他平台**：可通过创建新的适配器类支持其他AI聊天平台
2. **本地存储**：可添加本地存储功能，保存历史对话记录
3. **更多搜索选项**：支持高级搜索，如按时间、关键词权重等
4. **自定义样式**：添加用户可配置的样式选项

## 注意事项

- 插件在ChatGPT网页（https://chatgpt.com/*）和豆包网页（https://*.doubao.com/*）上生效
- 所有数据仅保存在本地，不上传服务器
- 插件会实时监听页面变化，可能会对性能有轻微影响

## 开发说明

### 主要文件

- `manifest.json`：插件配置文件
- `content.js`：核心功能实现，包含：
  - 适配器模式实现（ChatGPTAdapter和DoubaoAdapter）
  - 消息提取和处理逻辑
  - 文件附件识别
  - 可拖动调整大小功能
  - 跨平台支持（自动根据URL选择适配器）
- `style.css`：样式文件，包含：
  - 历史对话列表样式
  - 可拖动分隔条样式
  - 高亮动画效果
- `icon16.png`、`icon48.png`、`icon128.png`：插件图标

### 如何贡献

1. Fork本项目
2. 提交Pull Request
3. 等待审核

## 优化记录

### 2026-04-06 - Kimi平台支持
- **功能**：添加了对Kimi平台的完整支持
- **实现**：创建了KimiAdapter适配器类，实现了Kimi平台的消息提取和处理逻辑
- **特性**：
  - 自动平台检测，根据URL选择合适的适配器
  - 保持与ChatGPT相同的用户体验
  - 支持Kimi平台的文件消息处理
  - 统一的跨平台接口
  - 专门针对Kimi纯HTML结构的消息提取
  - 优先使用user-content类名识别用户消息
- **技术**：
  - 适配器模式设计
  - 多选择器策略识别用户消息（按优先级排序）
  - 智能消息过滤，基于类名、data属性、所有属性和文本内容
  - 实时平台检测
  - 三层消息提取策略（先基于user-content类名，再基于选择器，最后基于文本内容）
  - 缓存机制，避免重复处理元素
  - 错误处理，提高稳定性
- **改进**：
  - 新增extractMessagesByUserContent方法，优先使用user-content类名查找用户消息
  - 优化了选择器列表，将user-content放在首位，提高查找效率
  - 简化了isUserMessageByText方法，移除了对用户指令词汇的依赖，采用更宽松的判断
  - 降低了消息长度要求，从5个字符改为1个字符，避免错过短消息
  - 修改了默认判断逻辑，默认为用户消息，只排除明显的AI回复和页面元素
  - 增加了去重逻辑，避免重复消息
  - 使用文本内容分析排除AI回复和常见页面文本，提高用户消息识别率
  - 限制选择器查询和元素处理数量，提高性能
  - 增加了缓存机制，避免重复计算
  - 增加了错误处理，提高稳定性
  - 优化了选择器优先级，提高查找效率

### 2026-04-06 - 豆包平台支持
- **功能**：添加了对豆包平台的完整支持
- **实现**：创建了DoubaoAdapter适配器类，实现了豆包平台的消息提取和处理逻辑
- **特性**：
  - 自动平台检测，根据URL选择合适的适配器
  - 保持与ChatGPT相同的用户体验
  - 支持豆包平台的文件消息处理
  - 统一的跨平台接口
- **技术**：
  - 适配器模式设计
  - 基于data-testid的消息识别
  - 实时平台检测

## 许可证

MIT License